/**
 * Public portfolio credentials. This account is intentionally shared and must
 * never receive administrative privileges or contain private information.
 */
const demoAccount = Object.freeze({
  name: "Portfolio Demo",
  email: "demo@example.com",
  password: "OrbitDemo2026!",
  role: "user",
});

module.exports = { demoAccount };
